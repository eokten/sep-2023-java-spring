services:
  postgres:
    image: postgres:latest
    container_name: pg-local
    restart: always
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: "product-service-db"
      POSTGRES_USER: "product-service-db-user"
      POSTGRES_PASSWORD: "product-service-db-password"
    volumes:
      - ./pg_data:/var/lib/postgresql/data/

volumes:
  pg_data:
